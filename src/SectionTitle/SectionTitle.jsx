
const SectionTitle = ({ title }) => {
    return (
        <div className="border border-t-2 border-b-2 mb-10 p-5">
            <p className="text-3xl font-semibold text-center">{title}</p>
        </div>
    );
};

export default SectionTitle;